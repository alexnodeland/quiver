/// <reference types="vite/client" />

// Vite's `?url` asset imports (e.g. `import workletUrl from '@quiver-dsp/wasm/worklet?url'`)
// resolve to the emitted asset URL as a string. Covered by vite/client's `*?url`
// module declaration referenced above.
